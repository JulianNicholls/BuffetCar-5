<%= render 'shared/page_title', title: 'Edit your Profile' %>

<%= render 'form' %>

<div class="col-sm-4 col-sm-offset-4">
  <p class="text-center">
    [ <%= link_to 'Return to Profile', @chef, class: 'custom-link' %> ]
  </p>
</div>
